SELECT 
			ON.[ID], 
			ON.[NOTE], 
			ON.[IS_CUSTOMER_VISIBLE], 
			ON.[CREATED_DATE] 
FROM [ORDER_NOTES] 
			ON 
WHERE 
			ON.[IsDeleted] = 0 
	AND 
			ON.[ORDER_ID] = @PRM_ORDER_NOTE_ORDER_ID 
ORDER BY 
			ON.[CREATED_DATE] DESC 
OFFSET @StartIndex ROWS FETCH NEXT @PageSize ROWS ONLY 